-- Need optinals for Process_data.
-- Need a better code strcture and runtime ifs that would make sure 
      if the expected data to be created 
      for the client is actually created by the data thread.



























